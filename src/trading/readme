思路:
  1:每个角色身上存储当前剩余的钱，借出去的钱(map)，借入的钱(map)
  2:借钱和还钱 先检测参数是否合法，然后事物更新DB，之后更新缓存
  
  
接口，所有请求和返回数据都为json 
   1:	http:121.41.107.94:8889/createUser 
        功能:创建新角色
        请求参数:
                {
	                "init_money": 1000      //初始化的money
                }
        返回:
            成功:
				{
					"data":{
						"user_id":10001
					},
                    "state":0
				}
             失败:
			 	{
					"state":1,
					"error":"XXXXXXX"
				}
	2: 	http:121.41.107.94:8889/createBorrow 
		

